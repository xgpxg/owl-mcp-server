打包(linux)：
```shell
sh ./build/deploy.sh package <PACKAGE>
```
打包(windows)：
```shell
sh ./build/deploy-win.sh package <PACKAGE>
```

发布(linux)：
```shell
sh ./build/publish.sh <PACKAGE> linux
```
发布(windows)：
```shell
sh ./build/publish.sh <PACKAGE> windows
```